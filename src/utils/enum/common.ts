export enum EmployeeStatus {
  active = 'active',
  nonactive = 'nonactive',
}

export enum Status {
  pending = 'pending',
  approved = 'approved',
  rejected = 'rejected',
}

export enum AddressType {
  domicile = 'domicile',
  origin = 'origin',
  alternative = 'alternative',
}

export enum Gender {
  male = 'male',
  female = 'female',
}

export enum MaritalStatus {
  single = 'single',
  married = 'married',
  divorced = 'divorced',
  widowed = 'widowed',
}
